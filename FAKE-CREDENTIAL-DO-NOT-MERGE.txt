THIS FILE IS A DELIBERATE TEST FIXTURE FOR A SECRET-SCAN RED RUN.
Nothing here is a real credential. This branch is deleted immediately
after the scan goes red.

1. AWS's published documentation example key. gitleaks does NOT flag this:
   its aws-access-token rule carries an allowlist of `.+EXAMPLE$`.
   Kept here so the red run also documents why it is not enough.

aws_access_key_id = AKIAIOSFODNN7EXAMPLE
aws_secret_access_key = wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

2. What actually trips the scan: a PEM private-key block whose body is
   plain English, so it cannot be a key at all — only its envelope matches.

-----BEGIN RSA PRIVATE KEY-----
THIS IS NOT A KEY. IT IS THE WORDS "THIS IS NOT A KEY" REPEATED.
THIS IS NOT A KEY. IT IS THE WORDS "THIS IS NOT A KEY" REPEATED.
-----END RSA PRIVATE KEY-----
